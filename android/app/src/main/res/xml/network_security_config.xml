<?xml version="1.0" encoding="utf-8"?>
<!--
  O Android bloqueia HTTP em texto claro desde a API 28. O Flutter libera isso
  apenas no manifesto de debug, entao o APK de release falhava ao falar com a
  API local ("Nao foi possivel conectar ao servidor").

  Aqui liberamos o texto claro SOMENTE para os enderecos de desenvolvimento.
  Producao (Railway) usa HTTPS e continua sujeita ao bloqueio padrao.

  Para testar o APK em um celular fisico apontando para a sua maquina, adicione
  o IP da rede local abaixo (o `ipconfig` mostra qual e):
      <domain includeSubdomains="false">192.168.0.10</domain>
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <!-- "localhost" do Windows visto de dentro do emulador Android -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
