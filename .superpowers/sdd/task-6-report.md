# Task 6 — Shell de navegação + Agenda

## Status
Concluída. Foram implementados o shell com Agenda e Equipe, a agenda com onboarding, abas próximos/passados, destaque do próximo culto, estados de carregamento/erro/vazio e os stubs para o formulário e detalhe.

O roteador agora direciona usuários autenticados para `/agenda`; `/home` redireciona para essa rota. As rotas de equipe existentes foram preservadas dentro do shell.

## Commits
N/A (nenhum commit criado).

## Verificação
`flutter analyze` executado em `app`: `No issues found!`

## Observações
Os formulários e detalhes de culto permanecem intencionalmente como stubs para a Task 7. Não foram executados testes de widget nem build de APK, pois não foram solicitados nesta tarefa.
